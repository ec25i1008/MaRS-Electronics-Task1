This circuit uses the millis() function to blink three LEDs at different intervals simultaneously. 
Each LED has an independent timer that checks elapsed time and toggles the LED when its interval is reached. 
This non-blocking approach allows multiple LEDs to run at the same time.
